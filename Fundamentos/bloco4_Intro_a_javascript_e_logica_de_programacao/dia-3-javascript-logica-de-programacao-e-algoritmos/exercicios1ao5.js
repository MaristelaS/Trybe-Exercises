// 1. Faça um algoritmo que calcule a soma de 50 a 100 usando a estrutura “for” e retorne no formato:
// A soma de 50 a 100 é: X.
let soma = 0;
for (let index = 95; index <= 100; index += 1) {
  soma += index;
} 
console.log ("A soma de 50 a 100 é: ", soma);